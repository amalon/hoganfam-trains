#pragma once

#define CONCAT1(A, B)			A##B
#define CONCAT2(TRAIN, PART)		CONCAT1(TRAIN, PART)

#define MODEL_ITEM(TRAIN, PART)		MODEL_ITEM_##TRAIN(PART)
#define MODEL_DAMAGE(TRAIN, PART)	MODEL_DAMAGE_##TRAIN(PART)
#define MODEL_DATA(TRAIN, PART)		CONCAT2(TRAIN_##TRAIN, MODEL_DATA_##TRAIN(PART))

// Default item space
#define MODEL_ITEM_DEFAULT	GOLDEN_PICKAXE
#define MODEL_DAMAGE_DEFAULT	32

// Train types
#define TRAIN_C158		10
#define TRAIN_METRO		20
#define TRAIN_A4		30
